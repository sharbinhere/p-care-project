import 'package:flutter/material.dart';

class PatientsNeedsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Patients Needs')),
      body: const Center(child: Text('Patients Needs Screen')),
    );
  }
}